import { component$ } from '@builder.io/qwik';

interface ProjectReportPreviewProps {}

export const ProjectReportPreview = component$<ProjectReportPreviewProps>(() => {
	return <p>project preview</p>;
});
